{
  "name": "frontend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "browser-sync start --proxy 'localhost:3000' --files 'public/**/*.*' --startPath '/login.html'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
